import { describe, expect, it } from "vitest";

// Placeholder so the verify pipeline exercises vitest from the first commit.
// Replaced by real parser tests in the next commit.
describe("verify pipeline", () => {
  it("runs", () => {
    expect(true).toBe(true);
  });
});
